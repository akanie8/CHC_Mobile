import { View, Text } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const explore = () => {
  return (
    <SafeAreaView>
      <Text style={{fontSize: 20, fontFamily: 'Oswald'}}>explore</Text>
    </SafeAreaView>
  )
}

export default explore